using System;

class Program
{
    static void Main()
    {
        int[] arr = { 10, 45, 23, 89, 5 };
        int max = arr[0];

        foreach (int num in arr)
        {
            if (num > max)
                max = num;
        }

        Console.WriteLine("Max = " + max);
    }
}
