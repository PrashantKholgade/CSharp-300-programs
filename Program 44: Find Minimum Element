using System;

class Program
{
    static void Main()
    {
        int[] arr = { 10, 45, 23, 89, 5 };
        int min = arr[0];

        foreach (int num in arr)
        {
            if (num < min)
                min = num;
        }

        Console.WriteLine("Min = " + min);
    }
}
